name: Build flow-server image

# Builds the flow-server container image and pushes to GHCR.
#
# The server-only green-field rewrite landed on main in the rebuild cutover
# (2026-07-27), so both trigger branches are live: a push to main builds a
# digest-pinnable image tagged with the commit SHA plus `:latest`, a push to the
# rebuild integration branch tags `:rebuild` instead. Consumers that pin a digest
# (homelab-study) read it from the job summary either way.
#
# Multi-arch: linux/amd64 (cluster nodes) + linux/arm64 (Apple Silicon). QEMU
# emulates the off-host arch.

on:
  push:
    branches: [main, rebuild]
    paths:
      - 'cmd/flow-server/**'
      - 'internal/**'
      - 'web/tailwind.css'
      - 'deploy/podman/Dockerfile.server'
      - 'go.mod'
      - 'go.sum'
      - 'Makefile'
      - '.github/workflows/build-server-image.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Compute tags
        id: tags
        run: |
          set -euo pipefail
          OWNER=$(echo "${{ github.repository_owner }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="ghcr.io/${OWNER}/flow-server"
          TAGS="${IMAGE}:${{ github.sha }}"
          BRANCH="${GITHUB_REF##*/}"
          if [ "$BRANCH" = "main" ]; then
            TAGS="${TAGS},${IMAGE}:latest"
          else
            TAGS="${TAGS},${IMAGE}:${BRANCH}"
          fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "Computed tags: ${TAGS}"

      - name: Build and push
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: deploy/podman/Dockerfile.server
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.tags.outputs.tags }}
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.title=flow-server
          cache-from: type=gha,scope=flow-server
          cache-to: type=gha,mode=max,scope=flow-server

      - name: Surface image digest
        # Plan 2 (homelab-study, a separate GitOps repo) pins this digest by
        # hand, so echo it to the log and the job summary. Canonical per-line
        # appends to $GITHUB_STEP_SUMMARY (no brace group). No job-level
        # outputs block: no same-repo downstream job consumes it.
        run: |
          echo "Image digest: ${{ steps.build.outputs.digest }}"
          echo "### flow-server image" >> "$GITHUB_STEP_SUMMARY"
          echo "- Tags: \`${{ steps.tags.outputs.tags }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Digest: \`${{ steps.build.outputs.digest }}\`" >> "$GITHUB_STEP_SUMMARY"

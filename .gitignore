.claude/
bin/
dist/
coverage.out
coverage.html

# go build artifacts from smoke tests (the new server / mcp binaries land in
# the worktree root; the legacy `flow` binary at repo root is intentionally
# tracked).
/flow-server
/flow-mcp

# macOS Finder noise.
.DS_Store

# Claude project files (per global rule: never committed)
CLAUDE.md
CLAUDE-*.md

apiVersion: v1
kind: Service
metadata:
  name: {{ include "flow-server.fullname" . }}
  labels:
    {{- include "flow-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "flow-server.selectorLabels" . | nindent 4 }}

# M1 Slice 4 — Home Dashboard — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Turn the minimal `/` Home placeholder into the real dashboard: a **timer-hero** (Start immediately / Stop→project-picker), **general stats** (Heute/Woche/Monat saldo tiles + burndown banner — consuming the glue Slice 3 preserved), and **newest knowledge articles** — with SSE live-reload. (The activity-feed/logstream is the separate Slice 5.)

**Architecture:** Server-rendered templ + htmx + SSE. Home gets its OWN `/ui/home/start|stop` + `/ui/home` fragment handlers (the worktime hero hard-targets `/ui/worktime/*`+`#content`, so it's copied, not shared — extraction is a later refactor). The saldo/burndown VM glue (`statsSaldoTile`, `burndownBannerVM`, `BurndownBanner`) preserved in Slice 3 is consumed verbatim. No backend/usecase changes.

**Tech Stack:** Go, templ (`make generate`), Tailwind (`make web`). `make ci = lint verify-generate verify-css verify-no-popups cover build`.

## Global Constraints
- **Build discipline:** `.templ`→`make generate`+commit `_templ.go`; `tailwind.css`→`make web`+commit `app.css`. NO color-emoji (geometric ▶■● ok); NO `alert/confirm/prompt`.
- The full touchpoint detail + the recovered verbatim saldo-feeding pattern is in `.superpowers/sdd/slice4-touchpoints.md` — read it.
- **USE LIVE SSE event names** — `project.created` is DEAD (real event = `node.created`); use the real `session.*`/`document.*`/`settings.changed` names.
- Each task leaves `go build ./...` + `go test ./internal/...` green.
- All commands from `/Users/msoent/SourceCode/serverkraken/flow-m1`.

---

### Task 1: Timer-hero on Home (functional core: start/stop + live timer + SSE)

**Files:** `webui/home.templ` (hero + `#content` container), `webui/home_vm.go` (timer fields), `httpserver/webui_home.go` (`homeDataFor`, `handleHomeHome`, `handleHomeFragment`, `handleHomeStart`, `handleHomeStop`), `server.go` (routes), tests.

**Read first:** `heute.templ` (`heuteHero` L55-121, `heuteStartCard` L132-143, `heuteOuter` L27-34), `webui.go` (`handleWebStart` L15-21, `handleWebStop` L23-46), `webui_heute.go` (`heuteDataFor` L51-142). The map has the exact blocks.

- [ ] **Step 1 (test, RED):** handler tests — `GET /` (idle, no running session) renders the **start card** (a form POSTing to `/ui/home/start`); with a running session, renders the **hero** (live `data-timer` + a stop form POSTing to `/ui/home/stop` with the project picker). `POST /ui/home/start` starts a session (assert via `GetRunningSession`); `POST /ui/home/stop` with a nodeID stops it. Match the existing `webui_heute_test.go` harness.
- [ ] **Step 2:** `home_vm.go` — extend `HomeVM` with the timer fields the hero needs (mirror the subset of `HeuteVM` used by `heuteHero`/`heuteStartCard`: `Running *domain.WorkSession` (or its ID + the running flag), `RunningBase int`/`LoggedDur string` for the live timer, `Nodes []components.NodePickerItem` for the stop picker, plus a greeting field if used). Read `HeuteVM` to copy the exact field names/types the hero reads.
- [ ] **Step 3:** `home.templ` — copy `heuteHero`/`heuteStartCard` as `homeHero`/`homeStartCard`, changing `hx-post="/ui/worktime/start"`→`/ui/home/start`, `hx-post="/ui/worktime/stop"`→`/ui/home/stop` (keep `hx-target="#content"`). Wrap the whole Home content in `<div id="content" hx-get="/ui/home" hx-trigger="sse:session.started, sse:session.stopped, sse:session.updated, sse:session.deleted, sse:settings.changed, sse:document.created, sse:document.updated, sse:document.deleted" hx-swap="innerHTML">` (model on `heuteOuter`). `homeContent` renders: the timer-hero (`if vm.Running != nil { @homeHero(vm) } else { @homeStartCard(vm) }`). KEEP the 3 `homeSectionLink` cards below for now (Task 2/3 replace/augment them). The hero IS the Kristall hero card (resolves the Slice-3 "bare div" Minor) — wrap appropriately in glass.
- [ ] **Step 4:** `webui_home.go` — add `homeDataFor(ctx, u, errMsg) (webui.HomeVM, error)` mirroring `heuteDataFor`'s session part: `GetRunningSession`, `ListSessionsRange` (today, for `LoggedDur`), `ListNodes` (engagement filter for the picker). Guard with `if s.Stats.Sessions != nil` like Heute. Rewrite `handleHomeHome` to render via `homeDataFor`; add `handleHomeFragment` (renders a new `HomeFragment` = just the `#content` inner). Add `handleHomeStart`/`handleHomeStop` mirroring `handleWebStart`/`handleWebStop` (StartSession/StopSession + CreateNode-on-newProject + `Bus.Publish(EventSessionStarted/Stopped)`) but rendering the **Home** fragment.
- [ ] **Step 5:** `server.go` — register `GET /ui/home`→`handleHomeFragment`, `POST /ui/home/start`→`handleHomeStart`, `POST /ui/home/stop`→`handleHomeStop` (near the `/ui/worktime/*` routes).
- [ ] **Step 6:** `make generate`; `go build ./...`; `go test ./internal/adapter/...` → green.
- [ ] **Step 7:** Commit `feat(webui): Home timer-hero (start/stop + live timer + SSE reload)`.

---

### Task 2: Saldo tiles + burndown banner on Home

**Files:** `webui/home_vm.go` (saldo fields), `httpserver/webui_home.go` (`homeDataFor` stats wiring), `webui/home.templ` (tiles + banner), tests. **Consume the preserved glue** (`statsSaldoTile`, `burndownBannerVM`, `BurndownBanner`).

- [ ] **Step 1 (test, RED):** extend the Home handler test: `GET /` renders the 3 saldo tiles (Heute/Woche/Monat) + the burndown banner. Seed sessions+target so a tile shows a non-zero saldo (assert the formatted value present).
- [ ] **Step 2:** `home_vm.go` — add `TodaySaldo/TodayPos bool/TodaySub`, `WeekSaldo/WeekPos/WeekSub`, `MonthSaldo/MonthPos/MonthSub` (string/bool), `Burndown components.BurndownVM`.
- [ ] **Step 3:** `webui_home.go` `homeDataFor` — add the **verbatim recovered pattern** (see `.superpowers/sdd/slice4-touchpoints.md` §"Recovered saldo-feeding pattern"): `Stats.Today` + `Stats.Burndown` + `Stats.Week` (Mon–Fri loop excluding Sat/Sun) → the saldo strings via `webui.FmtSaldoVerbose`/`FmtVerbose` + `burndownBannerVM(burndown)` (callable directly — it's in the `httpserver` package). Guard with `if s.Stats.Sessions != nil`.
- [ ] **Step 4:** `home.templ` — add the saldo section (paste the recovered block: `<section class="grid ... sm:grid-cols-3 ...">` with 3× `@statsSaldoTile(...)` + a `@components.BurndownBanner(vm.Burndown)` section), placed below the timer-hero, inside `#content`.
- [ ] **Step 5:** `make generate`; `go build ./...`; `go test ./internal/adapter/...` → green.
- [ ] **Step 6:** Commit `feat(webui): Home saldo tiles + burndown (consume preserved glue)`.

---

### Task 3: Newest knowledge articles on Home

**Files:** `webui/home_vm.go` (`NewestDocs`), `webui/home_newest.go` or a `webui` helper (`BuildHomeNewest`), `httpserver/webui_home.go` (ListDocuments + nodeMaps wiring), `webui/home.templ` (cards), tests.

- [ ] **Step 1 (test, RED):** `GET /` renders newest-doc cards (given seeded docs) linking `/wissen/{id}`, newest first, capped at N (e.g. 5).
- [ ] **Step 2:** `home_vm.go` — `NewestDocs []DocRow` (the `DocRow` type already exists in `webui` — `doc_types.go`).
- [ ] **Step 3:** Build the rows inside the `webui` package (since `docRowFromDocument` is package-private): add a `BuildHomeNewest(docs []domain.Document, colors map[string]string, n int) []DocRow` helper (sort by UpdatedAt desc — pgstore already does, but sort defensively like `sortedDocuments`; cap N; map via `docRowFromDocument`). In `webui_home.go` `homeDataFor`: `docs,_ := s.ListDocuments.Execute(ctx,u.ID,nil,nil)`; `_,colors,_,_ := s.nodeMaps(ctx,u.ID)`; `vm.NewestDocs = webui.BuildHomeNewest(docs, colors, 5)`.
- [ ] **Step 4:** `home.templ` — a "Zuletzt im Wissen" section (`T(ctx,"wissen.latest")`) rendering `vm.NewestDocs` as cards (reuse the `wissenDocRow`-style markup or the `Latest` list; show the colored **project corner** via `DocRow.ProjectColor` — see `document.templ:52-53` swatch; or a `@components.CardCorner` if the hue maps to a token). Each links `/wissen/{id}`. Place inside `#content` below the saldo section. Replace the placeholder `homeSectionLink` cards from Slice 3 (the timer + stats + newest now ARE the Home content; keep a compact Projekte/Wissen entry only if it still reads well — your call, but the 3 generic placeholder links should go).
- [ ] **Step 5:** `make generate`; `go build ./...`; `go test ./internal/adapter/...` → green.
- [ ] **Step 6:** Commit `feat(webui): Home newest knowledge articles`.

---

### Task 4: Decouple Frei from the worktime sub-nav (Slice-3 coherence follow-up)

**Files:** the `worktimeSubnav` helper (drop the Frei tab), `webui/frei.templ` (drop the worktime subnav). Resolves the Slice-3 review note (Frei is a standalone SecondaryNav utility, not a worktime sub-page; without this it shows a double "Frei" cue).

- [ ] **Step 1:** `rg -n "func worktimeSubnav|worktimeSubnav\(" internal/adapter/webui` — remove the **Frei** entry from `worktimeSubnav` (so the Zeit strip = Heute·Woche·Historie). The Zeit pages (heute/woche/historie) pick this up automatically.
- [ ] **Step 2:** `frei.templ` — change `@components.AppShell("frei", nil, worktimeSubnav("frei"), freiOuter(vm))` → `@components.AppShell("frei", nil, nil, freiOuter(vm))` (Frei stands alone; the sidebar "Frei" highlight from Slice-3's SecondaryNav fix is its wayfinding).
- [ ] **Step 3:** `make generate`; `go build ./...`; `go test ./internal/adapter/...` → green (update any test asserting the Frei subtab or the worktimeSubnav 4th tab).
- [ ] **Step 4:** Commit `feat(webui): Frei standalone (drop from worktime sub-nav)`.

---

### Task 5: Wiring verification + done-gate

- [ ] **Step 1:** `go build ./... && go vet ./...` → clean.
- [ ] **Step 2:** `make web` + `git diff --exit-code static/app.css` → clean (commit if drift). `make generate` + `git diff --exit-code '*_templ.go'` → clean.
- [ ] **Step 3:** **`make ci`** → green. Report exact coverage % if it dips (don't add fake tests).
- [ ] **Step 4:** **Route audit:** `rg -n "mux.Handle" internal/adapter/httpserver/server.go` — confirm `GET /ui/home`, `POST /ui/home/start`, `POST /ui/home/stop` registered alongside the existing `/{$}` Home route.
- [ ] **Step 5:** Confirm Home consumes the preserved glue: `rg -n "statsSaldoTile|burndownBannerVM|BurndownBanner|BuildHomeNewest" internal/adapter` shows Home as a live caller (the Slice-3 review required these be consumed-or-deleted — they're now consumed).
- [ ] **Step 6:** **Manual done-gate (human, dev stack):** `/` shows the timer-hero (Start works; Stop opens the project picker; live timer ticks), the 3 saldo tiles + burndown banner (Privat excluded from the Soll if a `countsTowardTarget=false` engagement is booked), and the newest knowledge cards (clickable). SSE live-reload when a session starts/stops in another tab.
- [ ] **Step 7:** Commit any fixups.

---

## Self-Review (done)
1. **Coverage vs spec §5:** timer-hero start/stop (T1), general stats = saldo tiles + burndown (T2), newest wissen (T3), SSE live-reload (T1). Logstream/actor = Slice 5 (not here). Plus the Slice-3 follow-ups: Home hero is now a real Kristall card (T1 resolves the bare-div Minor), preserved glue consumed (T2 resolves the "consume-or-delete" requirement), Frei standalone (T4 resolves the double-cue).
2. **Placeholders:** the saldo-feeding is the verbatim recovered pattern (durable map §); the timer-hero is a named copy with explicit URL swaps; newest-wissen names the exact usecase + helper. The one judgment (keep/drop the Slice-3 section links) is called out in T3.
3. **Type consistency:** `HomeVM` grows across T1 (timer) → T2 (saldo) → T3 (NewestDocs), each additive. `burndownBannerVM`/`statsSaldoTile`/`DocRow`/`docRowFromDocument` reused with their existing signatures.

## Notes for the executor
- The timer-hero COPY duplicates `heuteHero`/`heuteStartCard`. This is deliberate for Slice 4 (the worktime hero hard-targets `/ui/worktime/*`); a shared `components.TimerHero(actionBase, ...)` extraction is a worthwhile FOLLOW-UP but out of scope here. Flag the duplication in the report.
- Watch the SSE event names — use the live ones (the map lists them); `project.created` does not exist.
- Home's live-timer needs NO new JS: `base.templ` already binds `[data-timer]` + rebinds on `htmx:afterSwap`.
